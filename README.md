# V-system thermostat QR decoder

Alternative of `http://qr.v-system.cz/?d=<data>`.

Usage: `./decode.py <url_file>`

`url_file` is a file where there is a decoded QR code (the URL), one on each line.
